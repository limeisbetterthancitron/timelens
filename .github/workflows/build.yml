name: Build

on:
  push:
    branches: ['**']
  pull_request:
  # Lets the build be started by hand, which is useful when a push has not triggered it.
  workflow_dispatch:

permissions:
  contents: read

jobs:
  build:
    name: Maven verify
    runs-on: ubuntu-latest

    steps:
      - name: Check out repository
        uses: actions/checkout@v4

      - name: Set up JDK 25
        uses: actions/setup-java@v4
        with:
          java-version: '25'
          distribution: temurin
          cache: maven

      - name: Build and test
        run: mvn --batch-mode verify

      - name: Upload plugin jar
        uses: actions/upload-artifact@v4
        with:
          name: TimeLens
          path: target/TimeLens-*.jar
          if-no-files-found: error
